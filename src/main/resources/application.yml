spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/student_test_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  cache:
    type: simple

server:
  port: 8080
